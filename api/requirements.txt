# Dependencias MÍNIMAS del backend desplegado, que sirve el snapshot precomputado.
#
# Con solo esto funcionan las rutas de lectura, el registro de feedback y el asistente;
# /inference/score degrada con un 503 explícito, que es el comportamiento diseñado y está
# cubierto por las pruebas de contrato.
#
# Versiones fijadas para que la imagen de Docker se construya siempre igual.

fastapi==0.139.2
uvicorn[standard]==0.51.0
pydantic==2.13.4

# Asistente conversacional: DeepSeek a través del SDK de OpenAI. Es una dependencia ligera,
# así que se incluye en la imagen base. El asistente solo responde si además está definida
# la variable de entorno DEEPSEEK_API_KEY (ver DEPLOY.md).
openai==2.46.0

# Para habilitar /inference/score (inferencia en vivo) haría falta añadir el stack de modelado
# completo —pandas, pyarrow, scikit-learn, xgboost, shap, joblib, ver requirements.txt de la
# raíz— y montar models/. Encarece mucho la imagen: solo si el plan de despliegue lo permite.
