# URL del backend desplegado (Fase 6). En desarrollo se puede dejar vacío:
# Vite redirige /api al backend local en http://127.0.0.1:8000 (ver vite.config.ts).
# En producción (Vercel/Netlify) es OBLIGATORIA, p. ej.:
# VITE_API_URL=https://<usuario>-nexus-sentinel.hf.space
VITE_API_URL=
